vagrant:<% services.forEach(function(service, i) { %>
  <%- service %>:
    params:
      user: vagrant
      mem: 1024
    hosts:
      1:
        ip: 192.168.60.<%- i + 1 %>0<% }); %>
<% envs.filter(function(env) { return env != 'vagrant';}).forEach(function(env) { -%>
# <%- env %>:<% services.forEach(function(service) { %>
#   <%- service %>:
#     params:
#       user:
#     hosts:
#       1:
#         ip:<% }); %>
<% }); -%>
