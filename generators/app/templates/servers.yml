<% envs.forEach(function(env) { %>
  <%= env %>:
  <% services.forEach(function(service) { %>
    <%= service %>:
      params:
        user:
      vms:
        1:
          ip:
  <% }); %>
<% }); %>
